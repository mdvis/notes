| 功能描述          | APT/dpkg                  | DNF/RPM                        | Pacman                           |
| :------------ | :------------------------ | :----------------------------- | :------------------------------- |
| **核心工具类型**    | `dpkg` (低级), `apt` (高级)   | `rpm` (低级), `dnf` (高级)         | `pacman` (兼具高低级)                 |
| **安装本地包**     | `sudo dpkg -i pkg.deb`    | `sudo rpm -ivh pkg.rpm`        | `sudo pacman -U pkg.pkg.tar.zst` |
| **安装仓库包**     | `sudo apt install pkg`    | `sudo dnf install pkg`         | `sudo pacman -S pkg`             |
| **卸载包**       | `sudo apt remove pkg`     | `sudo dnf remove pkg`          | `sudo pacman -R pkg`             |
| **卸载包及依赖**    | `sudo apt autoremove pkg` | `sudo dnf autoremove pkg`      | `sudo pacman -Rs pkg`            |
| **更新软件源列表**   | `sudo apt update`         | `sudo dnf check-update`        | `sudo pacman -Sy`                |
| **升级所有软件**    | `sudo apt upgrade`        | `sudo dnf upgrade`             | `sudo pacman -Syu`               |
| **搜索软件包**     | `apt search pattern`      | `dnf search pattern`           | `pacman -Ss pattern`             |
| **查看包信息**     | `apt show pkg`            | `dnf info pkg`                 | `pacman -Si pkg`                 |
| **列出已安装包**    | `dpkg -l`                 | `rpm -qa` `dnf list installed` | `pacman -Q`                      |
| **查询文件属于哪个包** | `dpkg -S /path/to/file`   | `rpm -qf /path/to/file`        | `pacman -Qo /path/to/file`       |
| **列出包内文件**    | `dpkg -L pkg`             | `rpm -ql pkg`                  | `pacman -Ql pkg`                 |
| **清理包缓存**     | `sudo apt clean`          | `sudo dnf clean all`           | `sudo pacman -Sc`                |
| **查找孤儿包**     | `deborphan` (需安装)         | `sudo dnf autoremove`          | `pacman -Qdtq`                   |



## apt

- **更新软件包列表**：
```bash
sudo apt update
```
此命令用于从软件源获取最新的软件包信息。
- **升级已安装的软件包**：
```bash
sudo apt upgrade
```
该命令会将已安装的软件包更新到最新版本。
- **安装软件包**：
```bash
sudo apt install <package_name>
```
用软件包名称替换 `<package_name>` 来安装指定软件包。
- **移除软件包**：
```bash
sudo apt remove <package_name>
```
这会移除指定的软件包，但会保留配置文件。
- **彻底移除软件包**：
```bash
sudo apt purge <package_name>
```
此命令不仅移除软件包，还会删除其配置文件。
- **自动清理不再需要的依赖包**：
```bash
sudo apt autoremove
```



## dnf

太棒了 ✅  
以下是整合后的完整版：

---

# 🧮 DNF 包管理命令索引速查大全

_(适用 Fedora / RHEL / CentOS Stream / Rocky / AlmaLinux)_

为运维、开发与 Linux 高级用户准备的结构化命令索引，  
涵盖 DNF 的全部核心与扩展功能（含 Copr、模块化、自动化、插件等）。

---

## 🏗️ 一、基础命令

|功能|命令|
|---|---|
|安装软件包|`dnf install pkg`|
|卸载软件包|`dnf remove pkg`|
|重新安装包|`dnf reinstall pkg`|
|安装本地包|`dnf install ./file.rpm`|
|仅下载不安装|`dnf download pkg`|
|安装源码构建依赖|`dnf builddep pkg.spec`|

### 📘 术语与概念说明

- **DNF (Dandified YUM)**：YUM 的继任者，更快且支持依赖事务管理与插件扩展。
    
- **软件包（Package）**：一个 `.rpm` 文件或仓库中的程序集合。
    
- **源码包（SRPM）**：包含源码和构建信息的 RPM 包，用于本地编译或打包。
    

---

## 🔍 二、查询与搜索

|功能|命令|
|---|---|
|按关键字搜索包|`dnf search keyword`|
|查看包详情|`dnf info pkg`|
|查看已安装包|`dnf list installed`|
|查看可用包|`dnf list available`|
|查看所有版本|`dnf list --showduplicates pkg`|
|查找文件归属|`dnf provides /usr/bin/file`|
|查询包中包含的文件|`dnf repoquery -l pkg`|
|查看包的依赖|`dnf repoquery --requires pkg`|
|查看依赖该包的包|`dnf repoquery --whatrequires pkg`|
|查看依赖树|`dnf repoquery --tree-requires pkg`|
|查询库文件来源|`dnf repoquery --whatprovides libfoo.so.1`|

### 📘 术语与概念说明

- **repoquery**：查询仓库中包的详细信息（依赖、提供、版本等）。
    
- **provides / whatprovides**：显示哪个包提供指定文件或库。
    
- **requires / whatrequires**：分析依赖关系，了解包间相互依赖。
    

---

## ⬆️ 三、系统更新与升级

|功能|命令|
|---|---|
|检查可更新软件|`dnf check-update`|
|更新所有包|`dnf upgrade`|
|更新指定包|`dnf upgrade pkg`|
|同步系统包版本|`dnf distro-sync`|
|仅下载更新|`dnf upgrade --downloadonly`|
|自动回答 “yes”|`dnf upgrade -y`|

### 📘 术语与概念说明

- **upgrade**：升级软件包至最新版本，自动解决依赖。
    
- **distro-sync**：同步系统包版本，使所有包匹配仓库中版本（修复系统不一致状态）。
    

---

## 🧩 四、缓存与元数据管理

|功能|命令|
|---|---|
|清理所有缓存|`dnf clean all`|
|清理下载的包|`dnf clean packages`|
|清理仓库元数据|`dnf clean metadata`|
|生成缓存|`dnf makecache`|
|查看缓存目录|`ls /var/cache/dnf`|

### 📘 术语与概念说明

- **缓存（cache）**：下载的包、索引、元数据存放在 `/var/cache/dnf/`。
    
- **元数据（metadata）**：仓库中关于包的索引信息，用于快速查询更新。
    

---

## 📦 五、软件包组与环境组

|功能|命令|
|---|---|
|列出组|`dnf group list`|
|查看隐藏组|`dnf group list --hidden`|
|查看组信息|`dnf group info "group"`|
|安装组|`dnf group install "group"`|
|删除组|`dnf group remove "group"`|

### 📘 术语与概念说明

- **包组（Group）**：功能相关的软件集合，如“Development Tools”。
    
- **环境组（Environment Group）**：由多个包组组成，如“Server with GUI”。
    

---

## 🧱 六、模块化（Modularity）

|功能|命令|
|---|---|
|列出模块|`dnf module list`|
|查看模块详情|`dnf module info php`|
|启用模块流|`dnf module enable php:7.4`|
|禁用模块流|`dnf module disable php`|
|重置模块|`dnf module reset php`|
|安装模块包|`dnf module install php:7.4/common`|

### 📘 术语与概念说明

- **模块（Module）**：提供软件的多个版本（流）并行管理。
    
- **模块流（Stream）**：特定版本分支，如 `php:7.4`。
    
- **启用 / 禁用 / 重置**：控制当前系统可见的模块流状态。
    

---

## 🧰 七、Copr 仓库管理（社区构建源）

|功能|命令|
|---|---|
|启用 Copr 仓库|`dnf copr enable user/project`|
|禁用 Copr 仓库|`dnf copr disable user/project`|
|删除 Copr 仓库|`dnf copr remove user/project`|
|查看 Copr 信息|`dnf copr info user/project`|
|列出启用的 Copr 仓库|`dnf copr list`|

### 📘 术语与概念说明

- **Copr（Cool Other Package Repo）**：Fedora 官方社区构建平台。
    
- **Copr 仓库**：由个人或团队托管的软件源，常用于发布实验性或第三方包。
    

---

## 🗃️ 八、仓库管理与配置

|功能|命令|
|---|---|
|查看启用仓库|`dnf repolist`|
|查看所有仓库（含禁用）|`dnf repolist all`|
|查看仓库详情|`dnf repoinfo`|
|启用指定仓库|`dnf config-manager --set-enabled repo`|
|禁用指定仓库|`dnf config-manager --set-disabled repo`|
|添加仓库|`dnf config-manager --add-repo URL`|
|临时启用仓库|`dnf --enablerepo=epel install htop`|
|临时禁用仓库|`dnf --disablerepo=baseos upgrade`|

### 📘 术语与概念说明

- **仓库（Repository）**：包来源定义，通常为远程镜像或本地路径。
    
- **EPEL 仓库**：Red Hat 系列常用的额外软件包源。
    
- **config-manager 插件**：用于启用、禁用或添加仓库配置。
    

---

## 🕓 九、历史与事务控制

|功能|命令|
|---|---|
|查看历史记录|`dnf history`|
|查看某次事务详情|`dnf history info ID`|
|撤销指定事务|`dnf history undo ID`|
|回滚到特定状态|`dnf history rollback ID`|
|查看用户安装的包|`dnf history userinstalled`|
|查看事务包差异|`dnf history diff ID1 ID2`|

### 📘 术语与概念说明

- **事务（Transaction）**：一次安装、升级或删除操作的完整过程。
    
- **历史记录（History）**：DNF 保存的事务日志，可用于回滚系统状态。
    

---

## 🧮 十、依赖与包关系分析

|功能|命令|
|---|---|
|查看依赖列表|`dnf deplist pkg`|
|查看依赖包|`dnf repoquery --requires pkg`|
|查看反向依赖|`dnf repoquery --whatrequires pkg`|
|查看依赖树|`dnf repoquery --tree-requires pkg`|
|检查系统依赖完整性|`dnf check`|

### 📘 术语与概念说明

- **依赖（Dependency）**：包运行所需的其他包。
    
- **反向依赖**：哪些包依赖当前包。
    
- **check / deplist**：检测系统依赖完整性与包依赖详情。
    

---

## 🧹 十一、系统维护与清理

|功能|命令|
|---|---|
|删除无用依赖|`dnf autoremove`|
|检查包一致性|`dnf check`|
|检查损坏依赖|`dnf check --dependencies`|
|删除缓存和旧包|`dnf clean all`|
|重建缓存|`dnf makecache`|

### 📘 术语与概念说明

- **autoremove**：清理不再需要的依赖包。
    
- **check**：检测包一致性和依赖关系是否损坏。
    

---

## 🔐 十二、版本锁与固定

|功能|命令|
|---|---|
|锁定包版本|`dnf versionlock add pkg`|
|查看锁定列表|`dnf versionlock list`|
|删除锁定|`dnf versionlock delete pkg`|
|清空所有锁定|`dnf versionlock clear`|

### 📘 术语与概念说明

- **版本锁（Version Lock）**：防止包被更新，确保系统稳定。
    
- **versionlock 插件**：负责添加、查看和移除锁定项。
    

---

## 🪵 十三、日志与调试

|功能|命令|
|---|---|
|查看日志|`less /var/log/dnf.log`|
|调试输出等级|`dnf -d 10 install pkg`|
|模拟安装|`dnf --assumeno install pkg`|
|测试事务|`dnf --setopt=tsflags=test upgrade`|
|静默模式|`dnf -q install pkg`|
|仅显示要执行的操作|`dnf -v upgrade`|

### 📘 术语与概念说明

- **调试等级（-d）**：控制输出的详细程度。
    
- **assumeno**：模拟执行操作而不真正修改系统。
    
- **dnf.log**：所有操作的日志文件，位于 `/var/log/dnf.log`。
    

---

## ⚙️ 十四、高级选项与参数

|参数|说明|
|---|---|
|`-y`|自动回答 yes|
|`--nobest`|允许安装非最高版本|
|`--best`|强制使用最高版本|
|`--skip-broken`|忽略依赖冲突|
|`--allowerasing`|允许移除冲突包以解决依赖|
|`--downloadonly`|仅下载不安装|
|`--setopt=key=value`|临时修改配置|
|`--releasever=8`|指定系统版本号（用于 repo）|
|`--enablerepo` / `--disablerepo`|启用/禁用仓库|

### 📘 术语与概念说明

- **nobest / best**：控制安装版本选择策略。
    
- **skip-broken**：在有依赖问题时继续执行操作。
    
- **setopt**：临时调整配置项，无需修改配置文件。
    
太好了 ✅  
继续为你输出完整版的后续部分 ——

---

# 🧩 DNF 包管理命令索引速查大全（下篇）

_(第十五章 ～ 第二十一章 · 完整扩展版)_

---

## 🧠 十五、插件（Plugins）管理

|功能|命令|
|---|---|
|查看可用插件|`dnf help plugins`|
|查看已启用插件|`dnf -v` 或 `dnf repolist -v`|
|启用指定插件|`dnf config-manager --setopt=pluginname=1`|
|禁用指定插件|`dnf config-manager --setopt=pluginname=0`|
|插件配置文件位置|`/etc/dnf/plugins/` 或 `/etc/dnf/plugins/*.conf`|

### 📘 常用插件一览

|插件名|功能说明|
|---|---|
|`versionlock`|锁定软件包版本，防止升级|
|`config-manager`|管理仓库配置（启用/禁用/新增）|
|`copr`|管理 Fedora Copr 仓库|
|`reposync`|同步远程仓库到本地镜像|
|`needs-restarting`|检查更新后是否需重启进程|
|`dnf-automatic`|定时自动更新系统|
|`download`|单独下载包而不安装|

### 📘 术语与概念说明

- **插件（Plugin）**：扩展 DNF 功能的小模块。
    
- **插件配置文件**：位于 `/etc/dnf/plugins/`，可单独启用或禁用。
    

---

## 🔄 十六、自动化与计划任务（DNF Automatic）

|功能|命令 / 配置文件|
|---|---|
|安装自动化工具包|`dnf install dnf-automatic`|
|主配置文件路径|`/etc/dnf/automatic.conf`|
|启用自动更新服务|`systemctl enable --now dnf-automatic.timer`|
|查看服务状态|`systemctl status dnf-automatic.timer`|
|手动触发自动更新|`systemctl start dnf-automatic.service`|
|查看日志输出|`journalctl -u dnf-automatic`|

### 📘 配置参数速览（`/etc/dnf/automatic.conf`）

|参数名|说明|
|---|---|
|`upgrade_type=default`|升级类型（`default` / `security`）|
|`apply_updates = yes`|是否自动应用更新|
|`download_updates = yes`|是否仅下载更新|
|`emit_via = motd/email`|通知方式|
|`random_sleep = 360`|延迟执行以平衡负载|

### 📘 术语与概念说明

- **dnf-automatic**：自动化更新工具，可设定定时任务。
    
- **timer 单元**：Systemd 计时器，用于周期性触发更新。
    

---

## ⚡ 十七、性能优化与下载加速

|功能|命令 / 设置|
|---|---|
|并行下载（推荐）|在 `/etc/dnf/dnf.conf` 添加：`max_parallel_downloads=10`|
|启用最快镜像选择|`fastestmirror=True`|
|跳过慢源重试次数|`retries=3`|
|仅下载更新（不安装）|`dnf upgrade --downloadonly`|
|使用 delta RPMs|`deltarpm=True`|
|关闭 delta RPMs（节省 CPU）|`deltarpm=False`|

### 📘 性能优化配置文件示例（`/etc/dnf/dnf.conf`）

```ini
[main]
gpgcheck=1
installonly_limit=3
clean_requirements_on_remove=True
fastestmirror=True
max_parallel_downloads=10
deltarpm=True
```

### 📘 术语与概念说明

- **delta RPM**：仅下载差异数据，减少带宽。
    
- **fastestmirror**：自动检测最快镜像源。
    
- **max_parallel_downloads**：并行下载数，提升速度。
    

---

## 🧱 十八、RPM 低层操作与 DNF 对照

|功能|DNF 命令|对应 RPM 命令|
|---|---|---|
|安装包|`dnf install pkg`|`rpm -ivh pkg.rpm`|
|升级包|`dnf upgrade pkg`|`rpm -Uvh pkg.rpm`|
|卸载包|`dnf remove pkg`|`rpm -e pkg`|
|查询包|`dnf info pkg`|`rpm -qi pkg`|
|查询文件归属|`dnf provides file`|`rpm -qf file`|
|列出文件|`dnf repoquery -l pkg`|`rpm -ql pkg`|
|检查依赖|`dnf check`|`rpm -Va`|
|验证包签名|自动完成|`rpm --checksig pkg`|

### 📘 术语与概念说明

- **RPM**：Red Hat 包管理底层系统。
    
- **DNF**：RPM 的高层前端，负责依赖解析与事务管理。
    
- **rpm -Va**：验证系统中包文件是否被修改。
    

---

## 🧾 十九、安全与签名验证

|功能|命令 / 文件路径|
|---|---|
|检查 GPG 签名状态|`dnf repoquery --requires gpg-pubkey`|
|导入 GPG 密钥|`rpm --import /etc/pki/rpm-gpg/RPM-GPG-KEY-*`|
|查看仓库签名设置|`cat /etc/yum.repos.d/*.repo`|
|启用全局签名验证|在 `/etc/dnf/dnf.conf` 中设置 `gpgcheck=1`|
|检查包签名|`rpm -K pkg.rpm` 或 `rpm --checksig pkg.rpm`|
|检查仓库证书（SSL）|`openssl s_client -connect mirror.repo:443`|

### 📘 术语与概念说明

- **GPG 签名**：确保软件包来源可信、内容未篡改。
    
- **gpgcheck=1**：强制 DNF 验证所有包签名。
    
- **repo GPG key**：仓库提供的公钥文件，用于验证包合法性。
    

---

## 🧮 二十、日志与路径参考

|类型|路径或命令|
|---|---|
|DNF 操作日志|`/var/log/dnf.log`|
|历史事务数据库|`/var/lib/dnf/history.sqlite`|
|缓存目录|`/var/cache/dnf/`|
|仓库配置文件|`/etc/yum.repos.d/*.repo`|
|主配置文件|`/etc/dnf/dnf.conf`|
|插件配置目录|`/etc/dnf/plugins/`|
|自动化配置文件|`/etc/dnf/automatic.conf`|
|Copr 源定义文件|`/etc/yum.repos.d/_copr_*`|
|RPM 数据库路径|`/var/lib/rpm/`|

---

## 🏆 二十一、Top 25 实用命令合集（建议收藏）

|类型|常用命令|
|---|---|
|安装与卸载|`dnf install pkg` / `dnf remove pkg`|
|更新系统|`dnf upgrade -y`|
|清理缓存|`dnf clean all`|
|搜索包|`dnf search keyword`|
|查看包详情|`dnf info pkg`|
|查看依赖|`dnf deplist pkg`|
|安装组|`dnf group install "Development Tools"`|
|启用仓库|`dnf config-manager --set-enabled epel`|
|禁用仓库|`dnf config-manager --set-disabled repo`|
|启用模块流|`dnf module enable php:7.4`|
|查看历史|`dnf history`|
|回滚事务|`dnf history rollback ID`|
|自动清理无用包|`dnf autoremove`|
|下载包但不安装|`dnf download pkg`|
|同步系统状态|`dnf distro-sync`|
|启用 Copr 源|`dnf copr enable user/project`|
|锁定版本|`dnf versionlock add pkg`|
|模拟执行|`dnf --assumeno install pkg`|
|查看缓存路径|`ls /var/cache/dnf`|
|查看日志|`less /var/log/dnf.log`|
|检查更新|`dnf check-update`|
|查看文件来源|`dnf provides /usr/bin/foo`|
|查看已安装包|`dnf list installed`|
|查看仓库详情|`dnf repoinfo`|
|生成缓存|`dnf makecache`|

---

## 📚 附录：配置与调优建议

```ini
# /etc/dnf/dnf.conf 推荐配置
[main]
gpgcheck=1
installonly_limit=3
clean_requirements_on_remove=True
fastestmirror=True
max_parallel_downloads=10
deltarpm=True
```


## pacman

- **同步软件包数据库并升级所有软件包**：
```bash
sudo pacman -Syu
```
此命令会同步软件包数据库并将所有可升级的软件包升级到最新版本。
- **安装软件包**：
```bash
sudo pacman -S <package_name>
```
使用软件包名称替换 `<package_name>` 来安装软件。
- **移除软件包**：
```bash
sudo pacman -R <package_name>
```
此命令用于移除指定的软件包，但会保留依赖它的其他软件包。
- **彻底移除软件包及其依赖**：
```bash
sudo pacman -Rs <package_name>
```
这会移除指定软件包及其不再被其他软件依赖的依赖包。
- **查找软件包**：
```bash
sudo pacman -Ss <keyword>
```
用关键词替换 `<keyword>` 来查找包含该关键词的软件包。 

