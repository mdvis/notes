* `-t` 测试配置，不运行仅检查配置文件语法
* `-s` 向 Nginx 主进程发送信号的命令
    *   reload 重载配置
    *   quit 平稳关闭，保存相关信息，有安排的关闭服务
    *   stop 快速关闭，可能不保存相关信息，迅速终止服务
    *   reopen 重新打开日志
* `-c <file>` 指定一个配置
* `-v` 版本
* `-V` 编译版本和配置参数