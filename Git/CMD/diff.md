- `--quiet` 不输出任何内容，只用“退出码”告诉你有没有差异;
	- `0` → 没有差异
	- `1` → 有差异
	- `>1` → 出错
- `--cached/--staged` 暂存内容和仓库差异
- `--name-only` 只显示文件名
- `--name-status` 显示修改类型 `M/A/D`
- `-w` 忽略空白变化
- `--stat`  单行显示 diff
- `-U` 上下文行数