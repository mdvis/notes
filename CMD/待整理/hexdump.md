`hexdump` 将二进制文件的内容打印为十六进制、人类可读的形式
常用选项：

| 选项          | 含义                  |
| ----------- | ------------------- |
| `-C`        | 经典格式（Canonical），最常用 |
| `-n N`      | 只显示前 N 个字节          |
| `-v`        | 不折叠重复行              |
| `-e FORMAT` | 自定义输出格式（例如按整型/浮点解析） |