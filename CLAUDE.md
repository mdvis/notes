## CLAUDE.md（术语上下文工程）
 通常包含
 - 常用 bash 命令
 - 核心文件与工具函数说明
 - 代码风格指南与测试指令
 - 仓库规范（分支命名，合并策略等）
 - 特定项目的警告或异常行为
**why 目的** 项目的目的及不同部分的功能，有助于决策一致性
**what 结构** 项目的地图，如单体仓库明确应用 apps 共享包的位置
**how 规范流程** 如何测试、类型检查、编译（构建、测试）步骤（nodejs 或 bun）、验证正确性
**原则** 
- 简洁
- 渐进式披露（根目录内容过多，子文件夹下的子 CLAUDE.md）
- 避免自动生成（缺乏针对性）
**区分工具职能** 如代码风格有 eslint prettier 处理，claude 不处理 linter 的工作
**stop  hook**？？

## 例子
写作-》素材-》多个方案的提纲-》并行给出每个方案的草稿（subagent）-》润色定稿

- 拆分
- 编排
- 存储（每步留痕，有迹可循）
- 分摊（传递路径而非内容）（存储部分将文件保存下来，将路径流入下个环节，避免整个文件流转浪费 token）
- 迭代
- 确定性的逻辑、内容交给脚本

## gemini skill
### MCP（Model Context Protocol）
- 链接外部系统，链接协议解决链接问题；所有工具都要加载到上下文，会导致上下文爆炸
- **Toos Search** 新技术，MCP 从预加载改为按需发现，工具定义超上下文 10% 自动启用，AI 需要某工具时先搜索再加载
### skills
- 复杂任务流程固化为可复用的 skill 组件（可复用的领域知识包）
- 按需调用，渐进式披露
- 包含指令、脚本、资源的文件夹，智能体发现并使用（相当于操作手册按需调用）
### skill 原理
- 渐进式披露（三层渐进式加载机制）
	1. 发现阶段：只加载元数据（名称、描述等）
	2. 激活阶段：任务相关时加载完整 SKILL.md
	3. 执行阶段：按需访问脚本和源文件
- SKILL.md 必需的，核心指令文档
- scripts 脚本：自带脚本无需上下文，无需加载到上下文，直接执行，脚本结果往往是确定性的
- reference 参考文档
- assets 源文件
### 场景
1. 重复指令
2. 特定领域规范
3. 复杂多步工作流
### 原则
- `$skill-name` 手动使用
- 单一职责
- 清晰的触发条件(描述清晰)
- 参数使用(提供描述和示例；描述如何获取，如何使用)
- 可组合性（其输出作为输入）
- 提供具体事例
#### 写好
1. 原子性
2. 给例子
3. 立规矩
	a. 定角色
	b. 拆步骤
	a. 划红线（能做、不能做）
4.  造接口（明确输入、输出格式 json，md 等）
5. 勤复盘
### 其他特性
- 外部工具（bash 等）
- 支持动态参数 `{param-name}`
- skill 可组合
- 条件执行
### 解决问题
| llm         | skills       |
| ----------- | ------------ |
| 缺乏领域专属上下文知识 | 定义专业领域规则和指令  |
| 缺乏标准化执行步骤   | 封装可重复使用的工作流程 |
| 特定工具链集成     | 集成自定义脚本和工具   |
| 项目特定最佳实践    | 提供领域特定上下文信息  |
### 其他
01 别迷信“最佳实践”，没有标准答案
02 像包工头一样工作：多 Agent 并行
03 选最强的模型：Opus > Sonnet
04 打造团队记忆：CLAUDE.md
05 先想清楚再动手：Plan 模式
06 拒绝重复劳动：斜杠命令与子 Agent
07 打通工具链：MCP 协议
08 长任务处理：让它自己跑，既然是长任务，就给它足够的自主权。
09 终极心法：给 AI 验证能力，Boris 说这是回报率最高的一条