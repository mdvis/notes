
## 1. 常见身份认证方式

### 1.1 用户名/密码认证

- **基本原理**：用户提供凭据，服务端校验后建立会话。密码存储必须使用加盐慢哈希（如 bcrypt、scrypt、Argon2），而非明文或快速哈希（MD5/SHA-256 直接哈希）。
- **适用场景**：几乎所有需要自有账户体系的 Web 应用、内部系统。
- **典型风险**：撞库、暴力破解、钓鱼、弱口令、拖库后密码泄露。
- **优缺点**：
  - 优点：实现成熟、用户理解成本低、无第三方依赖。
  - 缺点：密码管理成本高（重置、强度策略、泄露应急）；单独使用安全性有限，通常需要配合限流、锁定、MFA。

### 1.2 令牌认证

| 类型 | 原理 | 适用场景 | 关键注意点 |
|---|---|---|---|
| **Session Token**（不透明令牌） | 服务端存储会话状态，客户端只持有随机 ID（Cookie） | 传统 Web 应用、需要即时踢人/登出的场景 | 天然支持服务端撤销；需处理 CSRF；多实例部署需要会话存储（Redis 等） |
| **JWT**（自包含令牌） | 令牌本身携带声明（claims），由签名保证完整性 | 跨域/跨服务传递身份、无状态 API、微服务间透传 | **JWT 只保证完整性，不保证机密性**（payload 仅 Base64，非加密）；撤销困难，需短有效期 + 刷新令牌机制；注意 `alg=none`、密钥混淆等历史漏洞 |
| **API Token**（不透明访问令牌） | 服务端颁发的长期随机串，服务端校验（可查库或缓存） | 开放 API、CI/CD、服务对服务调用 | 支持即时撤销和权限范围（scope）划分；泄露风险高，需支持轮换、最小 scope、调用审计 |

**移动端注意**：移动端无法像浏览器一样安全保管 `HttpOnly Cookie` 中的长期秘密，常见做法是授权码流程 + PKCE 获取短期令牌，敏感凭据放入系统钥匙串/Keystore。

### 1.3 OAuth 2.0 / OpenID Connect

- **OAuth 2.0** 是**授权**框架（解决"第三方应用能否访问我的资源、能访问哪些范围"），不是认证协议。核心角色：资源拥有者、客户端、授权服务器、资源服务器。常用授权类型：授权码（+ PKCE，公共客户端首选）、客户端凭据（服务对服务）、刷新令牌。
- **OpenID Connect（OIDC）** 在 OAuth 2.0 之上增加了身份层（ID Token），才是"认证"协议，适用于"用 Google/GitHub 登录"这类联邦身份场景。
- **边界与选型**：
  - 只需要让用户授权第三方访问其数据 → OAuth 2.0。
  - 需要让用户用第三方账号登录你的系统 → OIDC。
  - 内部服务之间传递用户身份上下文 → JWT（通常由 OIDC 授权服务器签发）。
- **常见误区**：用隐式流程（implicit flow，已不推荐）、把 access token 当身份凭证长期缓存、自建 OAuth 而不复用成熟授权服务器。

### 1.4 双因素/多因素认证（2FA/MFA）

- **原理**：组合两类以上因素——所知（密码）、所有（手机、硬件密钥、TOTP App）、所是（生物特征）。
- **常见形态**：TOTP（如 Authenticator 类应用，推荐）、短信验证码（存在 SIM 劫持风险，作为兜底可接受）、WebAuthn/Passkey（抗钓鱼能力最强）、邮件验证码（安全性较弱）。
- **适用场景**：管理员账号、金融/支付操作、敏感数据访问、任何面向公网的用户系统。
- **工程要点**：MFA 状态必须与会话绑定（如 `amr` 声明）；提供备用恢复码并安全存储；防 MFA 轰炸（限制推送频率）。

### 1.5 单点登录（SSO）

- **原理**：一次登录，多系统通行。常见协议：OIDC/OAuth 2.0（现代首选）、SAML 2.0（企业市场仍大量存在，尤其对接大型企业客户时）、CAS（遗留系统）。
- **适用场景**：企业内多系统统一入口、SaaS 面向企业客户（B2B 常要求 SAML/OIDC 对接客户的 IdP）。
- **风险**：IdP 成为单点故障和单点攻击目标，需要 IdP 自身的高可用与强保护（IdP 管理员必须 MFA）。

### 1.6 API Key 与应用级认证

- **原理**：静态密钥标识应用/调用方，常放 Header 或查询参数。
- **特点**：实现极简，适合服务端对服务端、可信环境、低敏感度场景（如公开数据查询配额控制）。
- **风险**：无过期时间、无用户上下文、泄露即长期有效。工程上应：支持多 Key 轮换、按 Key 限流与审计、绝不放入前端代码或版本库。
- **与 API Token 的区别**：实践中边界模糊，但趋势是 API Key 偏向标识"应用"，OAuth 客户端凭据或短期 Token 偏向带权限范围的"访问凭证"。

### 1.7 场景选择速查

| 场景 | 推荐方案 |
|---|---|
| 传统 Web 应用（自有账号） | 密码 + 加盐慢哈希 + Session/短期 JWT + MFA |
| 移动端 | OIDC 授权码 + PKCE，令牌存系统安全存储 |
| 开放 API | OAuth 2.0 客户端凭据或 API Token（带 scope、可轮换、可审计） |
| 微服务内部 | mTLS 或短期 JWT 透传用户身份；服务身份与用户身份分开 |
| 企业 B2B SSO | OIDC 优先，兼容 SAML |

---

## 2. 主流加密机制

### 2.1 对称加密

- **概念**：加密与解密使用同一密钥。常见算法：AES（GCM 模式优先，提供加密+完整性）。
- **特点**：速度快，适合加密大量数据；核心难题是**密钥如何安全分发**。
- **应用**：数据库字段加密、文件加密、TLS 中实际加密数据通道的阶段。

### 2.2 非对称加密

- **概念**：公钥/私钥成对，公钥加密只有私钥能解，私钥签名可用公钥验证。常见算法：RSA、ECDSA、Ed25519、ECDH（用于密钥交换）。
- **特点**：解决密钥分发问题，但比对称加密慢得多，通常不直接加密大数据。
- **应用**：TLS 握手、数字签名、SSH 密钥、JWT 非对称签名（RS256/ES256）。

### 2.3 哈希算法

- **概念**：单向、定长输出，不可逆。
- **两类用途要区分**：
  - **完整性校验**：SHA-256 等（校验文件、去重、证书指纹）。
  - **密码存储**：必须用**加盐的慢哈希**（bcrypt/scrypt/Argon2），目的是提高暴力破解成本，普通快哈希不合格。
- **注意**：哈希不是加密（无法还原）。

### 2.4 消息认证码（HMAC）

- **概念**：哈希 + 共享密钥，验证消息**完整性 + 来源真实性**（持有同一密钥的双方之间）。
- **应用**：JWT 对称签名（HS256）、Webhook 签名验证、API 请求签名、TOTP 的底层构造。
- **与哈希的区别**：HMAC 有密钥，攻击者无法伪造合法摘要。

### 2.5 数字签名

- **概念**：用私钥对消息摘要签名，任何持有公钥者可验证——提供完整性、来源认证和**不可否认性**（HMAC 做不到，因为双方共享密钥）。
- **应用**：代码/制品签名、证书体系（CA 对证书签名）、OIDC 中授权服务器对 ID Token 签名（资源服务器用公钥验证，无需共享密钥）。

### 2.6 密钥交换与密钥管理

- **密钥交换**：ECDH/DHE 让双方在不安全信道上协商出共享密钥；TLS 1.3 的握手本质是"非对称交换 → 派生对称密钥 → 对称加密通信"，且默认前向保密（即使长期私钥泄露，历史会话不可解密）。
- **密钥管理基本思路**（工程重点）：
  - 密钥分级：用主密钥（KEK）加密数据密钥（DEK），即信封加密（envelope encryption）。
  - 密钥不落盘、不进代码和配置仓库，使用 KMS / HSM / 云密钥服务 / Vault 类产品。
  - 定期轮换，支持多版本密钥并存（新数据用新密钥，旧数据按版本解密）。
  - 密钥访问本身要有审计和最小权限。

### 2.7 在传输与存储中的典型应用

| 环节 | 机制 | 说明 |
|---|---|---|
| **数据传输** | TLS/HTTPS | 综合使用非对称交换 + 对称加密 + 数字签名（证书）；启用 HSTS；禁用过时协议版本 |
| **密码存储** | 加盐慢哈希 | bcrypt/Argon2，加盐抗彩虹表，慢哈希抗暴力破解 |
| **敏感字段加密** | 对称加密（AES-GCM）+ 信封加密 | 如身份证号、银行卡号；密钥放 KMS，考虑是否需要可搜索加密的取舍 |
| **接口防篡改** | HMAC / 数字签名 | Webhook、开放 API 请求签名 |
| **令牌** | HMAC 或数字签名 | 单服务内 HS256 足够；多方验证用 RS256/ES256 |

---

## 3. 方案对比与选型建议

### 3.1 认证方案对比

| 方案 | 安全性 | 实现复杂度 | 可撤销性 | 无状态/可扩展 | 兼容性 | 维护成本 |
|---|---|---|---|---|---|---|
| 密码 + Session | 中（依赖加固） | 低 | 强（服务端删会话） | 有状态，需共享会话存储 | 极好 | 中 |
| 密码 + JWT | 中 | 中 | 弱（需短有效期+黑名单/刷新机制） | 无状态，易扩展 | 好 | 中（密钥轮换、时钟、算法管控） |
| OAuth 2.0 / OIDC | 高（协议成熟） | 高（自建难，建议用成熟授权服务器） | 依赖刷新令牌与吊销端点 | 好 | 好 | 高，但集中化收益大 |
| MFA（TOTP/WebAuthn） | 显著提升 | 低-中 | — | — | TOTP 好；WebAuthn 现代浏览器支持 | 低 |
| API Key | 低-中 | 极低 | 支持（换 Key） | 好 | 极好 | 低，但泄露应急频繁 |
| mTLS | 高 | 高（证书生命周期管理） | 支持（CRL/短证书） | 好 | 服务端之间好，浏览器端差 | 高 |

### 3.2 加密机制对比（要点）

| 机制 | 性能 | 解决的核心问题 | 主要复杂度 |
|---|---|---|---|
| 对称加密（AES-GCM） | 快 | 数据机密性 + 完整性 | 密钥分发与轮换 |
| 非对称加密 | 慢 | 密钥分发、签名验证 | 证书/公钥信任链管理 |
| 哈希（SHA-256） | 快 | 完整性、指纹 | 无（但密码场景必须换慢哈希） |
| 慢哈希（Argon2 等） | 刻意慢 | 密码存储抗暴力破解 | 参数调优 |
| HMAC | 快 | 共享密钥下的完整性+来源 | 密钥共享导致不可否认性缺失 |
| 数字签名 | 慢 | 不可否认性、公开验证 | 私钥保护 |

### 3.3 场景化选型建议

- **内部管理系统**：密码 + Session/JWT + MFA（管理员强制）；若有企业 IdP 直接接 SSO。权限模型用 RBAC 起步。
- **面向用户的 Web 系统**：密码（Argon2）+ MFA 可选开启；或 OIDC 社会化登录降低密码管理负担。会话用 `HttpOnly + Secure + SameSite` Cookie。注意 CSRF 防护。
- **开放 API**：OAuth 2.0 客户端凭据或带 scope 的 Token；强制 HTTPS；按调用方限流；签名或 Token 短期有效；完整审计日志。
- **移动端**：授权码 + PKCE，令牌存系统安全存储（Keychain/Keystore），支持生物识别解锁本地凭据，实现设备绑定/吊销。
- **微服务架构**：服务身份用 mTLS 或工作负载身份（如 SPIFFE 思路）；用户身份通过 JWT 从网关透传，下游用公钥验证而非信任内网。区分"谁在调用（服务身份）"与"代表谁（用户身份）"。
- **敏感数据存储**：字段级 AES-GCM + KMS 信封加密；密码一律慢哈希；密钥访问审计；按合规要求考虑数据驻留与脱敏。
- **合规提示**：金融、医疗、政务等场景常有强制性合规要求（如密码算法、密钥管理、日志留存），选型前应先确认适用的监管要求，再在合规框架内做技术取舍。

---

## 4. 访问控制的经典设计原则

以下五条是信息安全领域（可追溯到 Saltzer & Schroeder 的安全设计原则等经典文献）在访问控制方向最常见的表述：

### 4.1 最小权限（Least Privilege）

- **核心含义**：主体只拥有完成其任务所必需的最小权限集合，不多给。
- **设计目的**：缩小凭证泄露或程序被攻破后的爆炸半径。
- **典型体现**：API Token 按 scope 颁发；数据库账号按表/操作授权；容器以非 root 运行；临时提权（如限时凭证）替代长期管理员权限。

### 4.2 默认拒绝（Fail-Secure / Deny by Default）

- **核心含义**：凡是未被明确允许的访问，一律拒绝；而不是未被禁止就放行。
- **设计目的**：避免因规则遗漏导致意外开放；新增资源自动处于受保护状态。
- **典型体现**：权限中间件默认拒绝未匹配的路由；防火墙白名单；新接口上线时默认无角色可访问，需显式授权。与之配套的是**安全失败**（fail-secure）：鉴权组件出错时应拒绝访问，而不是放行。

### 4.3 职责分离（Separation of Duties）

- **核心含义**：关键操作需要多个主体分别完成或共同确认，避免单点权力集中。
- **设计目的**：防止单人完成欺诈性操作，提高内部作恶成本。
- **典型体现**：财务付款的"发起-审批"分离；生产发布与审批分离；系统管理员、安全管理员、审计员分设；高敏感操作双人复核。

### 4.4 按需授权 / 权限分离（Need to Know / Privilege Separation）

- **核心含义**：即使是"有权限的人"，也只在其当前任务需要时才获得对应权限；系统层面将不同权限拆分到不同组件/进程。
- **设计目的**：最小权限在时间和维度上的延伸——限制权限的有效期与可见范围。
- **典型体现**：短期临时凭证（限时 STS 类令牌）；按项目/环境隔离权限；进程拆分后各自持有独立的最小凭据；敏感数据按需脱敏展示。

### 4.5 完整中介 / 审计可追溯（Complete Mediation & Auditability）

- **完整中介**：每次访问都必须经过权限检查，不能因"之前检查过"而缓存授权结论——防止权限回收后仍然生效、对象引用被篡改。典型体现：不缓存鉴权结果过久；对象访问每次校验所有权，防止通过遍历 ID 越权（IDOR）。
- **审计可追溯**：所有授权决策（尤其拒绝和敏感操作）应记录谁、何时、对什么、做了什么、结果如何，且审计者与被审计对象分离。典型体现：权限变更日志、管理员操作审计、不可篡改的审计流。

> 其他常见的相关原则还包括：机制经济性（权限机制应简单可验证）、心理可接受性（安全流程不能逼用户绕开）、开放设计（安全性不依赖算法保密）等，出自同一经典设计原则体系。

---

## 5. 主流访问控制模型

### 5.1 DAC（自主访问控制）

- **基本思想**：资源拥有者自主决定谁能访问自己的资源，并可转授。
- **授权依据**：资源拥有者的意愿（ACL/权限位）。
- **典型场景**：文件系统（Unix 权限位）、网盘分享链接、协作文档的共享设置。
- **优缺点**：灵活、用户自主；但权限扩散难以集中管控，易出现过度共享，安全性依赖用户自觉。
- **与认证/加密的关系**：依赖认证确认"拥有者"身份；加密（文件加密、分享密钥）可作为 DAC 之外的补充保护层。

### 5.2 MAC（强制访问控制）

- **基本思想**：由系统按统一安全策略强制控制访问，主体和资源都有安全标签/等级，用户不能更改。
- **授权依据**：安全等级与策略规则（如"不上读、不下写"的经典模型，或基于类别标签的规则）。
- **典型场景**：政府/军工等高安全场景；操作系统层面的 SELinux/AppArmor；容器隔离策略。
- **优缺点**：防泄露、防篡改能力强，抗内部威胁；但配置复杂、灵活性差，普通业务系统很少直接使用。
- **与认证/加密的关系**：通常与强认证绑定（如登录即确定安全级别）；数据分级常与加密策略联动（不同密级的数据用不同密钥/存储）。

### 5.3 RBAC（基于角色的访问控制）

- **基本思想**：不直接给用户授权，而是"用户 → 角色 → 权限"，通过角色聚合权限。
- **授权依据**：用户在组织中承担的角色（可含角色层级与约束，如互斥角色）。
- **典型场景**：绝大多数企业应用、管理后台、SaaS 的权限体系起步方案。
- **优缺点**：
  - 优点：直观映射组织结构，授权/回收批量高效，易审计，实现成本低。
  - 缺点：细粒度条件（时间、地点、数据归属）表达困难，角色膨胀（role explosion）是常见退化信号。
- **与认证/加密的关系**：认证确定"你是谁"，RBAC 决定"你能做什么"，两者天然衔接（会话中携带角色，中间件据此鉴权）；敏感角色操作建议叠加 MFA 与审计。

### 5.4 ABAC（基于属性的访问控制）

- **基本思想**：基于**主体属性、资源属性、环境属性、操作**的组合，按策略规则动态判定是否放行（如"医生可访问本科室且处于治疗期的病历"）。
- **授权依据**：属性 + 策略表达式，而非静态角色。
- **典型场景**：医疗、金融、多租户 SaaS 的行级数据权限、需要大量条件化规则的复杂业务。
- **优缺点**：
  - 优点：表达力强、粒度细、适应动态条件，规则集中管理。
  - 缺点：策略编写与维护复杂，策略正确性难以全局推理，调试与审计成本高，对策略引擎有依赖。
- **与认证/加密的关系**：认证提供的身份信息本身就是最重要的主体属性来源；属性断言（如 JWT claims）常在服务间传递供 ABAC 引擎使用；字段级加密可用于"即使绕过访问控制也无法读取"的纵深防御。

### 5.5 常见扩展模型（简述）

- **PBAC（基于策略的访问控制）**：常与 ABAC 混用，强调以集中式策略语言表达规则（如通用策略引擎/策略语言），适合跨系统统一管控。
- **ReBAC（基于关系的访问控制）**：以"主体与资源之间的关系图"为授权依据（如"你是这个文档所在空间的成员"），适合社交/协作类产品（文档、群组、层级资源），常见实现思路是关系元组存储 + 图遍历检查。
- **实践建议**：多数系统以 RBAC 起步，在出现明显条件化/数据归属需求时局部引入 ABAC 规则；协作型产品考虑 ReBAC；避免一开始就上最复杂的模型。

### 5.6 模型关系小结

```
认证（你是谁） → 授权模型（RBAC/ABAC/...：你能做什么） → 加密（即使前两者失效，数据仍受保护）
```

三者构成纵深防御：认证是入口，访问控制模型是主防线，加密是最后的数据保护层；审计贯穿三者，提供事后追溯与策略改进依据。

---

## 总结速查

| 问题 | 首选答案 |
|---|---|
| 密码怎么存 | Argon2/bcrypt 加盐慢哈希 |
| 会话怎么建 | Web 用 HttpOnly Cookie；API 用短期 JWT 或可撤销 Token |
| 第三方登录 | OIDC（授权码 + PKCE） |
| 服务间调用 | mTLS 或客户端凭据 + 短期令牌 |
| 传输安全 | 全链路 TLS，禁用明文回退 |
| 敏感字段 | AES-GCM + KMS 信封加密 |
| 权限模型 | RBAC 起步，条件复杂时叠加 ABAC/ReBAC |
| 设计底线 | 最小权限、默认拒绝、每次校验、全程审计 |

如需针对某个具体场景（如某类系统的认证与权限方案落地设计）展开，可以进一步讨论。